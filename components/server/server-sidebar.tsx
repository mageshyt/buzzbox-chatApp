import React, { FC } from "react";


interface ServerSidebarProps {
    serverId: string;
}
const ServerSidebar:FC<ServerSidebarProps> = ({serverId}) => {
  return <div></div>;
};

export default ServerSidebar;
